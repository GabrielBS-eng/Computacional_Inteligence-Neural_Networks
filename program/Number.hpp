#pragma once

std::default_random_engine rand;

class Number
{
  bool array[31];
  int weight[31];

public:
  Number();
  Number(int set);
  void  setArray(array[]);
  int*  getArray();
  void  setWeight(weight[]);
  int*  getWeight();
}
